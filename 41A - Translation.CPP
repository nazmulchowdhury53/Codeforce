#include<bits/stdc++.h>
#include<math.h>
#include<string.h>
#include<vector>
using namespace std;
int main()
{

    long long int a,b,c,d,i,j;
    string s,ss;
    cin>>s>>ss;
   // sort(s.begin(),s.end());
      reverse(s.begin(),s.end());
    if(s!=ss)
    {

        cout<<"NO"<<endl;
    }
    else
        cout<<"YES"<<endl;
}
